# Директория для сторонних зависимостей
# В будущем здесь могут быть подпроекты, такие как:
# - JSON парсер (nlohmann/json)
# - CSV парсер
# - Библиотеки для работы с PDF

message(STATUS "Third-party dependencies directory")
